<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /myportfolio/
  
  # 실제 파일이나 디렉토리가 존재하면 그대로 서빙
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  
  # 그 외의 모든 요청을 index.html로 리다이렉트
  RewriteRule . /myportfolio/index.html [L]
</IfModule>

